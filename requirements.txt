streamlit>=1.50
plotly>=5.22
pandas>=2.3
numpy>=2.0
scikit-learn>=1.5
# 이미지로 저장 필요할 때 (px.write_image 등)
kaleido>=0.2

# Parquet/Arrow 쓰면
pyarrow>=15

# 부스팅 모델을 실제로 쓰면(하나만 골라 추가)
xgboost>=2.1
lightgbm>=4.3
catboost>=1.2

docxtpl>=0.20
python-docx>=1.1
jinja2>=3.1
pillow>=10.0      # 템플릿에 이미지 넣거나 리사이즈할 때 안정적
lxml>=5.0         # 아래 docxcompose 쓸 때 필요
docxcompose>=1.4  # 여러 .docx를 한 파일로 병합할 때